import React from 'react'

const ContactDetails = ({className}:{className:string}) => {
  return (
    <div className={`${className}  rounded-lg`}>ContactDetails</div>
  )
}

export default ContactDetails