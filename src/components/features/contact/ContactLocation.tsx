import React from 'react'

const ContactLocation = ({className}:{className:string }) => {
  return (
    <div className={`${className}  rounded-lg`}>ContactLocation</div>
  )
}

export default ContactLocation